import { Player } from '@threekit-tools/treble/dist';

export const TopRightWidget = () => {
    return (
        <Player.TopRightWidgets>
            <span>top right widget</span>
        </Player.TopRightWidgets>
    );
};
