import { Player } from '@threekit-tools/treble/dist';

export const TopLeftWidget = () => {
    return (
        <Player.TopLeftWidgets>
            <span>top left widget</span>
        </Player.TopLeftWidgets>
    );
};
