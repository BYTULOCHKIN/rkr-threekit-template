import { Player } from '@threekit-tools/treble/dist';

export const BottomRightWidgets: React.FC = () => {
    return (
        <Player.BottomRightWidgets>
            <span>bottom right widgets</span>
        </Player.BottomRightWidgets>
    );
};
